Update documentation files that have become stale due to code changes on this branch.

## Workspace

{workspace_path}

## Project Guidelines

{guardrails}

Read `.forge/handoff.md` first (if it exists) to understand what was implemented and why. Then follow the update-docs skill process: get the diff, discover doc files, build the identifier checklist, grep for every identifier, evaluate candidates in two passes, and update confirmed stale docs.
